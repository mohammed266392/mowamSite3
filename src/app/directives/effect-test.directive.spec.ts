import { EffectTestDirective } from './effect-test.directive';

describe('EffectTestDirective', () => {
  it('should create an instance', () => {
    const directive = new EffectTestDirective();
    expect(directive).toBeTruthy();
  });
});
